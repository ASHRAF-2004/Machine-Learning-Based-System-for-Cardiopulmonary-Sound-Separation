@startuml
' Factory Method Pattern sequence diagram.
' Suggested LaTeX placement: normal page.

skinparam shadowing false
autonumber

participant "UploadRouter\n(Client)" as Router
participant "AudioValidatorFactory\n(Creator)" as Factory
participant "WavAudioValidator\n(ConcreteProduct)" as Validator

Router -> Factory : create(file.filename)

Factory -> Validator : instantiate for ".wav"
Validator --> Factory : validator instance
Factory --> Router : AudioValidator
Router -> Validator : validate(file)
Validator --> Router : valid WAV

@enduml
