\chapter{Proposed Design Patterns}
\sloppy

This chapter documents three design patterns used in the cardiopulmonary sound separation system. The roles are intentionally separated so that each pattern has its own purpose and its own main classes. \texttt{SeparationService} is explained as the Facade. \texttt{SeparationEngine} is explained as the Strategy Context. \texttt{AudioValidatorFactory} and \texttt{WavAudioValidatorFactory} are explained as the Factory Method Creator and ConcreteCreator used during upload validation.

\section{Facade Pattern}

The Facade Pattern provides a simplified interface to a group of related subsystem operations \parencite{gamma1994designpatterns}. In this project, it is used to keep the FastAPI separation route small while the service layer coordinates model lookup, storage paths, result recording, and job status updates.

\subsection{Pattern Type}

The Facade Pattern is a structural design pattern. It organises a complex workflow behind one higher-level interface that is easier for a client to call.

\subsection{Problem}

The separation endpoint should not directly handle model lookup, upload lookup, output path preparation, job creation, inference coordination, result persistence, and error handling. Placing all of that logic inside \texttt{SeparationRouter} would make the route difficult to test and difficult to explain in the software design report.

\subsection{Solution}

\texttt{SeparationRouter} calls \texttt{SeparationService} through one high-level operation. \texttt{SeparationService} acts as the Facade and coordinates the service-level subsystems: \texttt{ModelService}, \texttt{StorageService}, and \texttt{ResultService}. The router remains responsible for HTTP request and response handling, while the Facade owns the separation workflow.

\subsection{Structure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\textwidth]{facade_template.png}
  \caption{Facade Pattern Template Structure}
  \label{fig:facade-template}
\end{figure}

\begin{enumerate}
  \item \textbf{Client:} The caller that wants to use subsystem functionality without controlling every subsystem directly.
  \item \textbf{Facade:} The simplified interface that coordinates subsystem work.
  \item \textbf{Subsystems:} The internal components that perform specialised tasks.
\end{enumerate}

\subsection{Participants / Structure Explanation}

\begin{table}[H]
  \centering
  \small
  \caption{Facade Pattern Mapping}
  \label{tab:facade-mapping}
  \begin{tabularx}{\textwidth}{p{0.24\textwidth} p{0.36\textwidth} Y}
    \toprule
    \textbf{Pattern Participant} & \textbf{Project Class} & \textbf{Role in This Project} \\
    \midrule
    Client & \texttt{SeparationRouter} & Receives the \texttt{/separate/\{audio\_id\}} request and calls the Facade. \\
    Facade & \texttt{SeparationService} & Coordinates the high-level separation workflow and hides subsystem details from the route. \\
    Subsystem & \texttt{ModelService} & Retrieves the selected model or active default model from SQLite. \\
    Subsystem & \texttt{StorageService} & Resolves project paths and prepares heart/lung output paths. \\
    Subsystem & \texttt{ResultService} & Creates and reads result records used by result, history, and download views. \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Project Application}

\subsubsection{i. Function or Software Component Affected}

The affected component is the backend separation workflow in \texttt{app/services/separation\_service.py}. \texttt{SeparationRouter} in \texttt{app/routers/separation.py} delegates the workflow to the Facade instead of coordinating subsystem details itself.

\subsubsection{ii. Description of Workflow or Data Flow}

The user starts separation from the web interface. \texttt{SeparationRouter} receives the uploaded audio identifier and optional model identifier, then calls \texttt{SeparationService}. The Facade retrieves the upload and model, prepares output paths, creates a running job, coordinates the selected real separation strategy through the engine, records the result, and returns a response containing the generated heart and lung output paths. The Facade uses the strategy and model-selection helper internally, but its pattern role remains Facade only.

\subsubsection{iii. Sample Class Diagram}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{facade_project.png}
  \caption{Facade Pattern Project Class Diagram}
  \label{fig:facade-project}
\end{figure}

\subsubsection{iv. Facade Pattern Sequence Diagram}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{facade_sequence.png}
  \caption{Facade Pattern Sequence Diagram}
  \label{fig:facade-sequence}
\end{figure}

\subsubsection{v. Sample Potential Code}

\begin{lstlisting}[caption={Facade Pattern Code Example}]
class SeparationRouter:
    # Client: SeparationRouter calls the Facade.
    def separate_audio(self, audio_id: int, model_id: int | None, db):
        return SeparationService().separate_uploaded_audio(
            db=db,
            audio_id=audio_id,
            model_id=model_id,
        )


class SeparationService:
    # Facade: SeparationService provides one simple interface.
    def separate_uploaded_audio(self, db, audio_id, model_id=None):
        uploaded_audio = get_uploaded_audio(db, audio_id)
        # Subsystem: ModelService handles model selection.
        model = ModelService.get_model_for_separation(db, model_id)
        job = self.create_running_job(
            db, uploaded_audio.uploaded_audio_id, model.model_id
        )
        # Subsystem: StorageService handles file operations.
        output_paths = StorageService.build_separation_output_paths(job.job_id)
        output = run_selected_algorithm(uploaded_audio, model, output_paths)
        # Subsystem: ResultService handles result/history operations.
        result = ResultService.create_separation_result(db, job.job_id, output)
        return result

    def create_running_job(self, db, uploaded_audio_id, model_id):
        ...

    def mark_job_failed(self, db, job, error, processing_time_ms):
        ...
\end{lstlisting}

\subsubsection{vi. Benefits}

The Facade keeps route logic thin, makes the main separation use case easier to test, and gives the report a clear explanation of where workflow coordination happens. It also improves maintainability because storage, model lookup, and result persistence remain separate subsystem concerns.

\subsubsection{vii. Limitations}

The Facade can become too large if detailed validation, model internals, or result formatting are placed inside it. The implementation avoids this by keeping upload validation in its own component, model lookup in \texttt{ModelService}, output path handling in \texttt{StorageService}, and result creation in \texttt{ResultService}.

\section{Strategy Pattern}

The Strategy Pattern allows a family of algorithms to be used through a common interface \parencite{gamma1994designpatterns}. In this project, it keeps the separation engine independent from NeoSSNet internals while still using the current real NeoSSNet inference path.

\subsection{Pattern Type}

The Strategy Pattern is a behavioral design pattern. It defines a common interface for interchangeable algorithms and lets a context call the selected algorithm through that interface.

\subsection{Problem}

The backend should not spread NeoSSNet-specific inference calls across routers or service workflow code. If the workflow directly calls \texttt{run\_neossnet\_inference}, the system becomes harder to test and harder to extend when another real implemented separation algorithm is added.

\subsection{Solution}

\texttt{SeparationEngine} is introduced as the Strategy Context. It depends on the \texttt{SeparationAlgorithm} abstraction and calls \texttt{algorithm.separate(...)}. \texttt{NeoSSNetStrategy} implements the abstraction and wraps the existing real \texttt{run\_neossnet\_inference} function without duplicating NeoSSNet code.

\subsection{Structure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\textwidth]{strategy_template.png}
  \caption{Strategy Pattern Template Structure}
  \label{fig:strategy-template}
\end{figure}

\begin{enumerate}
  \item \textbf{Context:} The object that uses a strategy through a shared interface.
  \item \textbf{Strategy:} The abstraction that defines the algorithm operation.
  \item \textbf{ConcreteStrategy:} The implemented algorithm used at runtime.
\end{enumerate}

\subsection{Participants / Structure Explanation}

\begin{table}[H]
  \centering
  \small
  \caption{Strategy Pattern Mapping}
  \label{tab:strategy-mapping}
  \begin{tabularx}{\textwidth}{p{0.28\textwidth} p{0.28\textwidth} Y}
    \toprule
    \textbf{Pattern Participant} & \textbf{Project Class} & \textbf{Role in This Project} \\
    \midrule
    Context & \texttt{SeparationEngine} & Runs the selected algorithm through the abstraction. \\
    Strategy & \texttt{SeparationAlgorithm} & Defines the \texttt{separate(...)} contract and result shape. \\
    ConcreteStrategy & \texttt{NeoSSNetStrategy} & Calls the real NeoSSNet inference boundary and returns heart/lung output metadata. \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Project Application}

\subsubsection{i. Function or Software Component Affected}

The affected component is the ML inference boundary under \texttt{app/ml/}. The relevant files are \texttt{separation\_engine.py}, \texttt{separation\_algorithm.py}, \texttt{neossnet\_strategy.py}, and \texttt{neossnet\_inference.py}.

\subsubsection{ii. Description of Workflow or Data Flow}

\texttt{SeparationService} obtains the selected model and prepares the algorithm object used for the current separation request. The service passes the algorithm to \texttt{SeparationEngine}. The engine calls \texttt{algorithm.separate(...)} with the uploaded WAV path, model checkpoint path, model configuration path, and heart/lung output paths. \texttt{NeoSSNetStrategy} then calls the existing NeoSSNet inference function and returns output metadata.

\subsubsection{iii. Sample Class Diagram}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{strategy_project.png}
  \caption{Strategy Pattern Project Class Diagram}
  \label{fig:strategy-project}
\end{figure}

\subsubsection{iv. Strategy Pattern Sequence Diagram}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{strategy_sequence.png}
  \caption{Strategy Pattern Sequence Diagram}
  \label{fig:strategy-sequence}
\end{figure}

\subsubsection{v. Sample Potential Code}

\begin{lstlisting}[caption={Strategy Pattern Code Example}]
class SeparationAlgorithm(Protocol):
    # Strategy: SeparationAlgorithm defines the common interface.
    def separate(self, input_wav_path, model_path, model_config_path,
                 heart_output_path, lung_output_path, device_name="cpu"):
        ...


class NeoSSNetStrategy:
    # ConcreteStrategy: NeoSSNetStrategy implements the algorithm.
    def separate(self, input_wav_path, model_path, model_config_path,
                 heart_output_path, lung_output_path, device_name="cpu"):
        return run_neossnet_inference(
            input_wav_path=input_wav_path,
            model_path=model_path,
            model_config_path=model_config_path,
            heart_output_path=heart_output_path,
            lung_output_path=lung_output_path,
            device_name=device_name,
        )


class SeparationEngine:
    # Context: SeparationEngine uses a Strategy.
    def __init__(self, algorithm: SeparationAlgorithm):
        self.algorithm = algorithm

    def separate(self, **paths):
        return self.algorithm.separate(**paths)
\end{lstlisting}

\subsubsection{vi. Benefits}

The Strategy Pattern keeps NeoSSNet code isolated, makes the engine easy to test with a mocked algorithm, and allows the service workflow to remain stable when another real separation implementation is added later.

\subsubsection{vii. Limitations}

The current prototype mainly runs NeoSSNet, so the strategy layer must remain small. Overcomplicating the abstraction would make the student project harder to maintain without improving the current working inference path.

\section{Factory Method Pattern}

The Factory Method Pattern creates products through a creator class instead of forcing the client to instantiate concrete products directly \parencite{gamma1994designpatterns}. In this project, the pattern is used in the upload workflow to create the correct audio validator before the uploaded WAV file is saved.

\subsection{Pattern Type}

The Factory Method Pattern is a creational design pattern. It separates object creation from business workflow logic.

\subsection{Problem}

\texttt{UploadRouter} should not directly instantiate a concrete validator such as \texttt{WavAudioValidator}. If the route creates validators directly, upload request handling becomes tightly coupled to one validation implementation. Adding another validator later would require changing the route instead of extending the validation creation logic.

\subsection{Solution}

\texttt{AudioValidatorFactory} declares the factory method \texttt{create\_validator(filename)} and returns an \texttt{AudioValidator} product. \texttt{WavAudioValidatorFactory} is the concrete creator used by the current prototype. When the uploaded filename ends with \texttt{.wav}, it creates and returns a \texttt{WavAudioValidator}. \texttt{UploadRouter} uses the returned object through the \texttt{AudioValidator} product interface.

\subsection{Structure}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.82\textwidth]{factory_method_template.png}
  \caption{Factory Method Pattern Template Structure}
  \label{fig:factory-method-template}
\end{figure}

\begin{enumerate}
  \item \textbf{Client:} The code that needs a product but should not create the concrete product directly.
  \item \textbf{Creator:} The class that declares the factory method.
  \item \textbf{ConcreteCreator:} The implementation that creates a concrete product.
  \item \textbf{Product:} The interface returned to the client.
  \item \textbf{ConcreteProduct:} The implementation created by the factory method.
\end{enumerate}

\subsection{Participants / Structure Explanation}

\begin{table}[H]
  \centering
  \small
  \caption{Factory Method Pattern Mapping}
  \label{tab:factory-method-mapping}
  \begin{tabularx}{\textwidth}{p{0.28\textwidth} p{0.30\textwidth} Y}
    \toprule
    \textbf{Pattern Participant} & \textbf{Project Class} & \textbf{Role in This Project} \\
    \midrule
    Client & \texttt{UploadRouter} & Receives the uploaded file and asks the factory for a validator. \\
    Creator & \texttt{AudioValidatorFactory} & Declares \texttt{create\_validator(filename)} and returns an \texttt{AudioValidator}. \\
    ConcreteCreator & \texttt{WavAudioValidatorFactory} & Creates a WAV validator when the uploaded file extension is supported. \\
    Product & \texttt{AudioValidator} & Defines the common \texttt{validate(upload\_file)} operation used by the route. \\
    ConcreteProduct & \texttt{WavAudioValidator} & Validates the \texttt{.wav} extension and RIFF/WAVE file header. \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Project Application}

\subsubsection{i. Function or Software Component Affected}

The affected component is upload validation in \texttt{app/routers/upload.py} and \texttt{app/services/audio\_validation.py}. The factory creates a validator before the uploaded file is saved to local storage or inserted into SQLite.

\subsubsection{ii. Description of Workflow or Data Flow}

The user selects a WAV file and starts the upload flow. \texttt{UploadRouter} receives the file and calls \texttt{audio\_validator\_factory.create\_validator(file.filename)}. The concrete creator, \texttt{WavAudioValidatorFactory}, checks the file extension and returns a \texttt{WavAudioValidator}. The route then calls \texttt{validator.validate(file)} through the \texttt{AudioValidator} product interface before saving the file.

\subsubsection{iii. Sample Class Diagram}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{factory_method_project.png}
  \caption{Factory Method Pattern Project Class Diagram}
  \label{fig:factory-method-project}
\end{figure}

\subsubsection{iv. Factory Method Pattern Sequence Diagram}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{factory_method_sequence.png}
  \caption{Factory Method Pattern Sequence Diagram}
  \label{fig:factory-method-sequence}
\end{figure}

\subsubsection{v. Sample Potential Code}

\begin{lstlisting}[caption={Factory Method Pattern Code Example}]
class AudioValidator(Protocol):
    # Product: common validation interface.
    async def validate(self, upload_file):
        ...


class WavAudioValidator:
    # ConcreteProduct: validates WAV uploads.
    async def validate(self, upload_file):
        ...


class AudioValidatorFactory(ABC):
    # Creator: declares the factory method.
    @abstractmethod
    def create_validator(self, filename):
        ...


class WavAudioValidatorFactory(AudioValidatorFactory):
    # ConcreteCreator: creates the WAV validator.
    def create_validator(self, filename):
        if filename.endswith(".wav"):
            return WavAudioValidator()
        raise ValueError("Only .wav audio files are accepted.")


class UploadRouter:
    # Client: asks the Creator for a Product.
    async def upload_audio(self, file):
        validator = audio_validator_factory.create_validator(file.filename)
        await validator.validate(file)
\end{lstlisting}

\subsubsection{vi. Benefits}

The Factory Method keeps validator creation separate from upload request handling. It makes the upload route easier to maintain because the route depends on the \texttt{AudioValidator} product interface rather than directly constructing \texttt{WavAudioValidator}.

\subsubsection{vii. Limitations}

The current prototype has one concrete validator, \texttt{WavAudioValidator}, because the first version accepts only WAV files. The factory should remain small unless more audio formats or validation rules are added later.
\fussy
